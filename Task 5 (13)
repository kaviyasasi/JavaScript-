let cart = ["Laptop", "Mouse", "Keyboard"];

// Add "Monitor"
cart.push("Monitor");

// Remove "Mouse"
cart.splice(1, 1);

// Add "Headset" at the beginning
cart.unshift("Headset");

// Remove the last item
cart.pop();

// Print final cart
console.log(cart);
